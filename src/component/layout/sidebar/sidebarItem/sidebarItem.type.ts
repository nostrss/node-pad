export interface IPropsSidebarItem {
  list: {
    text: string;
    createdAt: any;
    itemId: string;
  };
}

export interface IPropsSidebarItemUI {
  list: {
    text: string;
    createdAt: any;
    itemId: string;
  };
}
